import Header from "./Header";
import "./Hero.css";
import Section from "./Section";

const Hero = () => {
  return (
    <Section id="hero">
      <div className="hero">
        <Header />
        meow
      </div>
    </Section>
  );
};

export default Hero;
